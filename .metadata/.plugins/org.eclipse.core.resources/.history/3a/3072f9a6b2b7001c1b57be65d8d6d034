package com.revature.p2.myp2.ctrl;

public enum TransType {

    DEPOSIT(1), WITHDRAWAL(2);
    int id;

    TransType(int id) {
        this.id = id;
    }

    public int getId() {
        return id;
    }

}